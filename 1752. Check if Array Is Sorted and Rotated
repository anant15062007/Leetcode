class Solution:
    def check(self, nums: List[int]) -> bool:
        
        # nums1 = list(dict.fromkeys(nums))
        # sorted_nums = sorted(nums1)
        # print(sorted_nums)
        # index = nums1.index(sorted_nums[0])
        # print(index)

        # if nums1[index::]+nums1[:index] == sorted_nums:
        #     return True
        # else:
        #     return False


        sorted_nums = sorted(nums)

        for i in range(len(nums)):
            if nums[i::]+nums[:i] == sorted_nums:
                return True
        return False
