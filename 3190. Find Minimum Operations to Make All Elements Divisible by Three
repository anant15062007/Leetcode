class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        
        n = len(nums)
        count = 0

        for i in nums:
            if i%3!=0:
                count+=1
            else:
                continue

        return count
