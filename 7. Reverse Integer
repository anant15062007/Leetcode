class Solution:
    def reverse(self, x: int) -> int:          
            
            
        length = len(str(abs(x)))
        y = abs(x)
        print(length)
        result = 0

        for i in range(1,length+1):
            rem = y%10
            y = y//10
            result = result + rem*(10**(length-i))


        if x < 0:
            result = result*(-1)
        else:
            result = result

        print(-(2**31))
        print((2**31)-1)

        if -(2**31)<=result and result<=(2**31)-1:
            return result
        else:
            return 0
